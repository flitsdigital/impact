# Implementation Plans

Generated by the improve skill (deep audit) on 2026-06-11, at commit
`4887fcb` (working tree contained uncommitted design-system edits in
`components/ui/` — noted per-plan where relevant). Execute in the order below
unless dependencies say otherwise. Each executor: read the plan fully before
starting, honor its STOP conditions, and update your row when done.

Verification baseline for all plans: `npx tsc --noEmit` (passes today),
`pnpm build`, `npx eslint .` (36 pre-existing problems — the bar is "no
new problems", not zero). `pnpm test` exists only after plan 003.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| [001](001-anon-rls-exposure.md) | Stop anonymous read access to klanten/profiles/posts | P1 | M | — | DONE (reviewed 2026-06-11; branch `advisor/001-anon-rls-exposure` @ cc7507b — awaiting merge + operator: set SUPABASE_SERVICE_ROLE_KEY, apply migration 010) |
| [002](002-private-project-docs.md) | Make project-docs storage bucket private (signed URLs) | P1 | M | — | DONE (reviewed 2026-06-11; branch `advisor/002-private-project-docs` @ d82bdad — awaiting merge + operator: apply migration 011) |
| [003](003-verification-baseline.md) | Verification baseline: scripts, vitest, date-helper tests, CI | P1 | M | — | DONE (reviewed 2026-06-11; branch `advisor/003-verification-baseline` @ af4dffe; 11/11 tests pass in UTC+Amsterdam; approved deviation: pnpm-workspace.yaml placeholders → true) |
| [004](004-server-action-hardening.md) | Harden server actions (auth gap, swallowed errors, filenames) | P2 | S | — (003 nice-to-have) | DONE (reviewed 2026-06-11; branch `advisor/004-server-action-hardening` @ 4274eed — merge note: trivial conflict with 002 in uploadProjectFile expected) |
| [005](005-dependency-hygiene.md) | Dependency & lockfile hygiene (dual lockfile, unused deps, PostCSS) | P2 | S | — | DONE-UNMERGED (reviewed 2026-06-11; branch `advisor/005-dependency-hygiene` @ 834fe16, branched from 4887fcb — merge needs manual resolution in package.json/pnpm-lock/calendar.tsx + clean tree) |
| [006](006-facturatie-crud.md) | Create/edit/delete for klant_facturen on the timeline | P2 | M | — | DONE-UNMERGED (reviewed 2026-06-11; branch `advisor/006-facturatie-crud` @ f910cab — merge blocked by concurrent uncommitted edits to FacturatieTijdlijn.tsx; deviation: Input type=date i.p.v. DatePicker, overweeg follow-up) |
| [007](007-klant-detail-page.md) | Klant detail page /klanten/[id] (fixes live dead link) | P2 | M | — | DONE (reviewed 2026-06-11, 1 revision round; branch `advisor/007-klant-detail-page` @ 4622188 — smoke test against live DB pending operator) |
| [008](008-post-scheduling-spike.md) | SPIKE: decide the post-scheduling completion | P3 | M | — | DONE (result: [008-RESULT-post-scheduling.md](008-RESULT-post-scheduling.md) — recommends overdue-surfacing now, defers automation, rejects auto-publish) |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale)

## Execution round 2026-06-11 (execute all)

Five executors ran in isolated worktrees off `4887fcb`, all APPROVED after review
(007 after one revision: ProjectStatusBadge used invented status values).
Worktrees live under `.claude/worktrees/agent-*`; each branch is unmerged —
**merging is the operator's decision.** Suggested merge order: 003 → 001 → 002 →
004 → 007 (004 has a trivial expected conflict with 002 in `uploadProjectFile`;
003 conflicts trivially with the uncommitted WIP's package.json/pnpm-lock —
resolve by re-running `pnpm install`).

Operator actions required after merging:
1. Add `SUPABASE_SERVICE_ROLE_KEY` to `.env.local` (Supabase dashboard → Project Settings → API → service_role) — plan 001.
2. Apply migrations 010 + 011 (`supabase db push` or SQL editor), then run the anon REST probe from plan 001 Step 5 and the public-object probe from plan 002 Step 5.
3. Consider rotating the anon key (full-table reads were possible until migration 010 runs).
4. Smoke-test `/klanten/[id]` and the bijlagen-flow against the live DB.
5. Commit the design-system WIP (DatePicker/calendar/popover + package.json), then re-run execute for plans 005 and 006.

## Dependency notes

- 001 and 002 are independent but both end with a Supabase migration apply —
  batching them into one `supabase db push` saves a round-trip.
- 004 composes with 002 in `uploadProjectFile` (002 changes the return value
  to a storage path; 004 sanitizes the filename within it). Either order
  works; if both run, the later executor must re-read that function.
- 003 should land before any future refactor of `ProjectDetailModule.tsx`
  (723 lines — refactor deliberately NOT planned this round) and unlocks
  `pnpm test` gates referenced by 004/006.
- 006 and 007 touch disjoint files; 007's Facturatie card can host 006's
  modal later, but neither blocks the other.

## Findings considered and rejected (do not re-audit)

- **".env.local / tsconfig.tsbuildinfo / next-env.d.ts committed to git"** —
  false; `git ls-files` shows none are tracked. They exist on disk only.
- **"Klanten list page should filter by status like the content page"** —
  showing all klanten (incl. gearchiveerd) on the klanten page is plausibly
  by design; not a bug.
- **GanttView `Promise.resolve().then()` in the drag mouseup** — deliberate
  side-effect-outside-updater pattern from commit `4c76388`; the
  rapid-double-drag race is theoretical.
- **AuthProvider unawaited promise chains** — React 19 ignores stale
  unmounted updates; real-world impact negligible; not worth a plan.
- **`post_logs.action = 'rescheduled'` violating a CHECK constraint** — no
  constraint exists on that column (the value list is only a comment).
- **RLS `using (true)` for authenticated users on all tables** — by design
  for a single-team internal tool; becomes a real finding only when
  multi-tenant/roles work starts (see deferred direction item below).
- **CVE-2025-29927 middleware bypass** — does not apply to Next.js 16.2.6.

## Deferred (real, but not planned this round)

- Lint debt: 26 errors (20× `no-explicit-any`, 6× `set-state-in-effect`) +
  10 warnings — CI (plan 003) runs lint non-blocking until paid down.
- `ProjectDetailModule.tsx` decomposition (723 lines) — needs the 003
  baseline first; revisit next run.
- Onboarding docs (README rewrite, Supabase setup guide) — plan 001 Step 6
  delivers `.env.example`; the rest deferred.
- Unbounded list queries / pagination (klanten, taken) — irrelevant at
  current data volumes; revisit at ~1000+ rows.
- Missing FK indexes (`posts.klant_id`, `posts.assignee_id`,
  `post_logs.user_id`) — cheap, but harmless at current scale; bundle into
  the next migration someone writes.
- Per-resource authorization in server actions (IDOR hardening) — only
  meaningful after a roles/tenancy model exists.
- Gebruikers/instellingen pages (sidebar links 404 today) — part of the
  larger team-management direction; needs a product decision on roles.
